em python:

for i in range(len(lista)-2):
    menor = i 
    for j in range(i+1, len(lista))
        if lista[menor] < lista[j]
            menor = j
    if menor != i:
        aux = lista[menor]
        lista[menor] = lista [j]
        lista[j] = lista[menor]

